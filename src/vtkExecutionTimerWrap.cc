/* this file has been autogenerated by vtkNodeJsWrap */
/* editing this might proof futile */

#define VTK_WRAPPING_CXX
#define VTK_STREAMS_FWD_ONLY
#include <nan.h>


#include "vtkObjectWrap.h"
#include "vtkExecutionTimerWrap.h"
#include "vtkAlgorithmWrap.h"

using namespace v8;

extern Nan::Persistent<v8::Object> vtkNodeJsNoWrap;
Nan::Persistent<v8::FunctionTemplate> VtkExecutionTimerWrap::ptpl;

VtkExecutionTimerWrap::VtkExecutionTimerWrap()
{ }

VtkExecutionTimerWrap::VtkExecutionTimerWrap(vtkSmartPointer<vtkExecutionTimer> _native)
{ native = _native; }

VtkExecutionTimerWrap::~VtkExecutionTimerWrap()
{ }

void VtkExecutionTimerWrap::Init(v8::Local<v8::Object> exports)
{
	Nan::SetAccessor(exports, Nan::New("vtkExecutionTimer").ToLocalChecked(), ConstructorGetter);
	Nan::SetAccessor(exports, Nan::New("ExecutionTimer").ToLocalChecked(), ConstructorGetter);
}

void VtkExecutionTimerWrap::ConstructorGetter(
	v8::Local<v8::String> property,
	const Nan::PropertyCallbackInfo<v8::Value>& info)
{
	InitPtpl();
	info.GetReturnValue().Set(Nan::New(ptpl)->GetFunction());
}

void VtkExecutionTimerWrap::InitPtpl()
{
	if (!ptpl.IsEmpty()) return;
	v8::Local<v8::FunctionTemplate> tpl = Nan::New<v8::FunctionTemplate>(New);
	VtkObjectWrap::InitPtpl( );
	tpl->Inherit(Nan::New<FunctionTemplate>(VtkObjectWrap::ptpl));
	tpl->SetClassName(Nan::New("VtkExecutionTimerWrap").ToLocalChecked());
	tpl->InstanceTemplate()->SetInternalFieldCount(1);

	Nan::SetPrototypeMethod(tpl, "GetClassName", GetClassName);
	Nan::SetPrototypeMethod(tpl, "getClassName", GetClassName);

	Nan::SetPrototypeMethod(tpl, "GetElapsedCPUTime", GetElapsedCPUTime);
	Nan::SetPrototypeMethod(tpl, "getElapsedCPUTime", GetElapsedCPUTime);

	Nan::SetPrototypeMethod(tpl, "GetElapsedWallClockTime", GetElapsedWallClockTime);
	Nan::SetPrototypeMethod(tpl, "getElapsedWallClockTime", GetElapsedWallClockTime);

	Nan::SetPrototypeMethod(tpl, "GetFilter", GetFilter);
	Nan::SetPrototypeMethod(tpl, "getFilter", GetFilter);

	Nan::SetPrototypeMethod(tpl, "IsA", IsA);
	Nan::SetPrototypeMethod(tpl, "isA", IsA);

	Nan::SetPrototypeMethod(tpl, "NewInstance", NewInstance);
	Nan::SetPrototypeMethod(tpl, "newInstance", NewInstance);

	Nan::SetPrototypeMethod(tpl, "SafeDownCast", SafeDownCast);
	Nan::SetPrototypeMethod(tpl, "safeDownCast", SafeDownCast);

	Nan::SetPrototypeMethod(tpl, "SetFilter", SetFilter);
	Nan::SetPrototypeMethod(tpl, "setFilter", SetFilter);

	ptpl.Reset( tpl );
}

void VtkExecutionTimerWrap::New(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	if(!info.IsConstructCall())
	{
		Nan::ThrowError("Constructor not called in a construct call.");
		return;
	}

	if(info.Length() == 0)
	{
		vtkSmartPointer<vtkExecutionTimer> native = vtkSmartPointer<vtkExecutionTimer>::New();
		VtkExecutionTimerWrap* obj = new VtkExecutionTimerWrap(native);		obj->Wrap(info.This());
	}
	else
	{
		Nan::Utf8String s(info[0]);
		if(info[0]->ToObject() != vtkNodeJsNoWrap )
			Nan::ThrowError("Parameter Error");
	}

	info.GetReturnValue().Set(info.This());
}

void VtkExecutionTimerWrap::GetClassName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetClassName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkExecutionTimerWrap::GetElapsedCPUTime(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	double r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetElapsedCPUTime();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkExecutionTimerWrap::GetElapsedWallClockTime(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	double r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetElapsedWallClockTime();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkExecutionTimerWrap::GetFilter(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	vtkAlgorithm * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetFilter();
		VtkAlgorithmWrap::InitPtpl();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::FunctionTemplate>(VtkAlgorithmWrap::ptpl)->GetFunction();
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkAlgorithmWrap *w = new VtkAlgorithmWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkExecutionTimerWrap::IsA(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		int r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->IsA(
			*a0
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkExecutionTimerWrap::NewInstance(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	vtkExecutionTimer * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->NewInstance();
		VtkExecutionTimerWrap::InitPtpl();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::FunctionTemplate>(VtkExecutionTimerWrap::ptpl)->GetFunction();
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkExecutionTimerWrap *w = new VtkExecutionTimerWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkExecutionTimerWrap::SafeDownCast(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject() && (Nan::New(VtkObjectWrap::ptpl))->HasInstance(info[0]))
	{
		VtkObjectWrap *a0 = ObjectWrap::Unwrap<VtkObjectWrap>(info[0]->ToObject());
		vtkExecutionTimer * r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->SafeDownCast(
			(vtkObject *) a0->native.GetPointer()
		);
			VtkExecutionTimerWrap::InitPtpl();
		v8::Local<v8::Value> argv[1] =
			{ Nan::New(vtkNodeJsNoWrap) };
		v8::Local<v8::Function> cons =
			Nan::New<v8::FunctionTemplate>(VtkExecutionTimerWrap::ptpl)->GetFunction();
		v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
		VtkExecutionTimerWrap *w = new VtkExecutionTimerWrap();
		w->native.TakeReference(r);
		w->Wrap(wo);
		info.GetReturnValue().Set(wo);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkExecutionTimerWrap::SetFilter(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkExecutionTimerWrap *wrapper = ObjectWrap::Unwrap<VtkExecutionTimerWrap>(info.Holder());
	vtkExecutionTimer *native = (vtkExecutionTimer *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject() && (Nan::New(VtkAlgorithmWrap::ptpl))->HasInstance(info[0]))
	{
		VtkAlgorithmWrap *a0 = ObjectWrap::Unwrap<VtkAlgorithmWrap>(info[0]->ToObject());
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetFilter(
			(vtkAlgorithm *) a0->native.GetPointer()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

