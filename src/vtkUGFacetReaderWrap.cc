/* this file has been autogenerated by vtkNodeJsWrap */
/* editing this might proof futile */

#define VTK_WRAPPING_CXX
#define VTK_STREAMS_FWD_ONLY
#include <nan.h>


#include "vtkPolyDataAlgorithmWrap.h"
#include "vtkUGFacetReaderWrap.h"
#include "vtkObjectWrap.h"
#include "vtkIncrementalPointLocatorWrap.h"

using namespace v8;

extern Nan::Persistent<v8::Object> vtkNodeJsNoWrap;
Nan::Persistent<v8::Function> VtkUGFacetReaderWrap::constructor;
Nan::Persistent<v8::FunctionTemplate> VtkUGFacetReaderWrap::ptpl;

VtkUGFacetReaderWrap::VtkUGFacetReaderWrap()
{ }

VtkUGFacetReaderWrap::VtkUGFacetReaderWrap(vtkSmartPointer<vtkUGFacetReader> _native)
{ native = _native; }

VtkUGFacetReaderWrap::~VtkUGFacetReaderWrap()
{ }

void VtkUGFacetReaderWrap::Init(v8::Local<v8::Object> exports)
{
	if (!constructor.IsEmpty()) return;
	Nan::HandleScope scope;

	v8::Local<v8::FunctionTemplate> tpl = Nan::New<v8::FunctionTemplate>(New);
	VtkPolyDataAlgorithmWrap::Init( exports );
	tpl->Inherit(Nan::New<FunctionTemplate>(VtkPolyDataAlgorithmWrap::ptpl));

	tpl->SetClassName(Nan::New("VtkUGFacetReaderWrap").ToLocalChecked());
	tpl->InstanceTemplate()->SetInternalFieldCount(1);
	InitTpl(tpl);

	constructor.Reset( tpl->GetFunction() );
	ptpl.Reset( tpl );

	exports->Set(Nan::New("vtkUGFacetReader").ToLocalChecked(),tpl->GetFunction());
	exports->Set(Nan::New("UGFacetReader").ToLocalChecked(),tpl->GetFunction());
}

void VtkUGFacetReaderWrap::InitTpl(v8::Local<v8::FunctionTemplate> tpl)
{
	Nan::SetPrototypeMethod(tpl, "CreateDefaultLocator", CreateDefaultLocator);
	Nan::SetPrototypeMethod(tpl, "createDefaultLocator", CreateDefaultLocator);

	Nan::SetPrototypeMethod(tpl, "GetClassName", GetClassName);
	Nan::SetPrototypeMethod(tpl, "getClassName", GetClassName);

	Nan::SetPrototypeMethod(tpl, "GetFileName", GetFileName);
	Nan::SetPrototypeMethod(tpl, "getFileName", GetFileName);

	Nan::SetPrototypeMethod(tpl, "GetLocator", GetLocator);
	Nan::SetPrototypeMethod(tpl, "getLocator", GetLocator);

	Nan::SetPrototypeMethod(tpl, "GetMerging", GetMerging);
	Nan::SetPrototypeMethod(tpl, "getMerging", GetMerging);

	Nan::SetPrototypeMethod(tpl, "GetNumberOfParts", GetNumberOfParts);
	Nan::SetPrototypeMethod(tpl, "getNumberOfParts", GetNumberOfParts);

	Nan::SetPrototypeMethod(tpl, "GetPartNumber", GetPartNumber);
	Nan::SetPrototypeMethod(tpl, "getPartNumber", GetPartNumber);

	Nan::SetPrototypeMethod(tpl, "IsA", IsA);
	Nan::SetPrototypeMethod(tpl, "isA", IsA);

	Nan::SetPrototypeMethod(tpl, "MergingOff", MergingOff);
	Nan::SetPrototypeMethod(tpl, "mergingOff", MergingOff);

	Nan::SetPrototypeMethod(tpl, "MergingOn", MergingOn);
	Nan::SetPrototypeMethod(tpl, "mergingOn", MergingOn);

	Nan::SetPrototypeMethod(tpl, "NewInstance", NewInstance);
	Nan::SetPrototypeMethod(tpl, "newInstance", NewInstance);

	Nan::SetPrototypeMethod(tpl, "SafeDownCast", SafeDownCast);
	Nan::SetPrototypeMethod(tpl, "safeDownCast", SafeDownCast);

	Nan::SetPrototypeMethod(tpl, "SetFileName", SetFileName);
	Nan::SetPrototypeMethod(tpl, "setFileName", SetFileName);

	Nan::SetPrototypeMethod(tpl, "SetLocator", SetLocator);
	Nan::SetPrototypeMethod(tpl, "setLocator", SetLocator);

	Nan::SetPrototypeMethod(tpl, "SetMerging", SetMerging);
	Nan::SetPrototypeMethod(tpl, "setMerging", SetMerging);

	Nan::SetPrototypeMethod(tpl, "SetPartNumber", SetPartNumber);
	Nan::SetPrototypeMethod(tpl, "setPartNumber", SetPartNumber);

}

void VtkUGFacetReaderWrap::New(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	if(!info.IsConstructCall())
	{
		Nan::ThrowError("Constructor not called in a construct call.");
		return;
	}

	if(info.Length() == 0)
	{
		vtkSmartPointer<vtkUGFacetReader> native = vtkSmartPointer<vtkUGFacetReader>::New();
		VtkUGFacetReaderWrap* obj = new VtkUGFacetReaderWrap(native);		obj->Wrap(info.This());
	}
	else
	{
		Nan::Utf8String s(info[0]);
		if(info[0]->ToObject() != vtkNodeJsNoWrap )
			Nan::ThrowError("Parameter Error");
	}

	info.GetReturnValue().Set(info.This());
}

void VtkUGFacetReaderWrap::CreateDefaultLocator(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->CreateDefaultLocator();
}

void VtkUGFacetReaderWrap::GetClassName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetClassName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkUGFacetReaderWrap::GetFileName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetFileName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkUGFacetReaderWrap::GetLocator(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	vtkIncrementalPointLocator * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetLocator();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkIncrementalPointLocatorWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkIncrementalPointLocatorWrap *w = new VtkIncrementalPointLocatorWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkUGFacetReaderWrap::GetMerging(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetMerging();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkUGFacetReaderWrap::GetNumberOfParts(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetNumberOfParts();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkUGFacetReaderWrap::GetPartNumber(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetPartNumber();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkUGFacetReaderWrap::IsA(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		int r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->IsA(
			*a0
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkUGFacetReaderWrap::MergingOff(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->MergingOff();
}

void VtkUGFacetReaderWrap::MergingOn(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->MergingOn();
}

void VtkUGFacetReaderWrap::NewInstance(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	vtkUGFacetReader * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->NewInstance();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkUGFacetReaderWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkUGFacetReaderWrap *w = new VtkUGFacetReaderWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkUGFacetReaderWrap::SafeDownCast(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkObjectWrap *a0 = ObjectWrap::Unwrap<VtkObjectWrap>(info[0]->ToObject());
		vtkUGFacetReader * r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->SafeDownCast(
			(vtkObject *) a0->native.GetPointer()
		);
		v8::Local<v8::Value> argv[1] =
			{ Nan::New(vtkNodeJsNoWrap) };
		v8::Local<v8::Function> cons =
			Nan::New<v8::Function>(VtkUGFacetReaderWrap::constructor);
		v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
		VtkUGFacetReaderWrap *w = new VtkUGFacetReaderWrap();
		w->native.TakeReference(r);
		w->Wrap(wo);
		info.GetReturnValue().Set(wo);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkUGFacetReaderWrap::SetFileName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetFileName(
			*a0
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkUGFacetReaderWrap::SetLocator(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkIncrementalPointLocatorWrap *a0 = ObjectWrap::Unwrap<VtkIncrementalPointLocatorWrap>(info[0]->ToObject());
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetLocator(
			(vtkIncrementalPointLocator *) a0->native.GetPointer()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkUGFacetReaderWrap::SetMerging(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetMerging(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkUGFacetReaderWrap::SetPartNumber(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkUGFacetReaderWrap *wrapper = ObjectWrap::Unwrap<VtkUGFacetReaderWrap>(info.Holder());
	vtkUGFacetReader *native = (vtkUGFacetReader *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetPartNumber(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

