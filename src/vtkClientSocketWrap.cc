/* this file has been autogenerated by vtkNodeJsWrap */
/* editing this might proof futile */

#define VTK_WRAPPING_CXX
#define VTK_STREAMS_FWD_ONLY
#include <nan.h>


#include "vtkSocketWrap.h"
#include "vtkClientSocketWrap.h"
#include "vtkObjectWrap.h"

using namespace v8;

extern Nan::Persistent<v8::Object> vtkNodeJsNoWrap;
Nan::Persistent<v8::Function> VtkClientSocketWrap::constructor;

VtkClientSocketWrap::VtkClientSocketWrap()
{ }

VtkClientSocketWrap::VtkClientSocketWrap(vtkSmartPointer<vtkClientSocket> _native)
{ native = _native; }

VtkClientSocketWrap::~VtkClientSocketWrap()
{ }

void VtkClientSocketWrap::Init(v8::Local<v8::Object> exports)
{
	Nan::HandleScope scope;

	v8::Local<v8::FunctionTemplate> tpl = Nan::New<v8::FunctionTemplate>(New);
	tpl->SetClassName(Nan::New("VtkClientSocketWrap").ToLocalChecked());
	tpl->InstanceTemplate()->SetInternalFieldCount(1);

	VtkObjectBaseWrap::InitTpl(tpl);
	VtkObjectWrap::InitTpl(tpl);
	VtkSocketWrap::InitTpl(tpl);
	InitTpl(tpl);

	constructor.Reset( tpl->GetFunction() );

	exports->Set(Nan::New("vtkClientSocket").ToLocalChecked(),tpl->GetFunction());
	exports->Set(Nan::New("ClientSocket").ToLocalChecked(),tpl->GetFunction());
}

void VtkClientSocketWrap::InitTpl(v8::Local<v8::FunctionTemplate> tpl)
{
	Nan::SetPrototypeMethod(tpl, "ConnectToServer", ConnectToServer);
	Nan::SetPrototypeMethod(tpl, "connectToServer", ConnectToServer);

	Nan::SetPrototypeMethod(tpl, "GetClassName", GetClassName);
	Nan::SetPrototypeMethod(tpl, "getClassName", GetClassName);

	Nan::SetPrototypeMethod(tpl, "IsA", IsA);
	Nan::SetPrototypeMethod(tpl, "isA", IsA);

	Nan::SetPrototypeMethod(tpl, "NewInstance", NewInstance);
	Nan::SetPrototypeMethod(tpl, "newInstance", NewInstance);

	Nan::SetPrototypeMethod(tpl, "SafeDownCast", SafeDownCast);
	Nan::SetPrototypeMethod(tpl, "safeDownCast", SafeDownCast);

}

void VtkClientSocketWrap::New(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	if(!info.IsConstructCall())
	{
		Nan::ThrowError("Constructor not called in a construct call.");
		return;
	}

	if(info.Length() == 0)
	{
		vtkSmartPointer<vtkClientSocket> native = vtkSmartPointer<vtkClientSocket>::New();
		VtkClientSocketWrap* obj = new VtkClientSocketWrap(native);		obj->Wrap(info.This());
	}
	else
	{
		Nan::Utf8String s(info[0]);
		if(info[0]->ToObject() != vtkNodeJsNoWrap )
			Nan::ThrowError("Parameter Error");
	}

	info.GetReturnValue().Set(info.This());
}

void VtkClientSocketWrap::ConnectToServer(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkClientSocketWrap *wrapper = ObjectWrap::Unwrap<VtkClientSocketWrap>(info.Holder());
	vtkClientSocket *native = (vtkClientSocket *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		if(info.Length() > 1 && info[1]->IsInt32())
		{
			int r;
			if(info.Length() != 2)
			{
				Nan::ThrowError("Too many parameters.");
				return;
			}
			r = native->ConnectToServer(
				*a0,
				info[1]->Int32Value()
			);
			info.GetReturnValue().Set(Nan::New(r));
			return;
		}
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkClientSocketWrap::GetClassName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkClientSocketWrap *wrapper = ObjectWrap::Unwrap<VtkClientSocketWrap>(info.Holder());
	vtkClientSocket *native = (vtkClientSocket *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetClassName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkClientSocketWrap::IsA(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkClientSocketWrap *wrapper = ObjectWrap::Unwrap<VtkClientSocketWrap>(info.Holder());
	vtkClientSocket *native = (vtkClientSocket *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		int r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->IsA(
			*a0
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkClientSocketWrap::NewInstance(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkClientSocketWrap *wrapper = ObjectWrap::Unwrap<VtkClientSocketWrap>(info.Holder());
	vtkClientSocket *native = (vtkClientSocket *)wrapper->native.GetPointer();
	vtkClientSocket * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->NewInstance();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkClientSocketWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkClientSocketWrap *w = new VtkClientSocketWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkClientSocketWrap::SafeDownCast(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkClientSocketWrap *wrapper = ObjectWrap::Unwrap<VtkClientSocketWrap>(info.Holder());
	vtkClientSocket *native = (vtkClientSocket *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkObjectWrap *a0 = ObjectWrap::Unwrap<VtkObjectWrap>(info[0]->ToObject());
		vtkClientSocket * r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->SafeDownCast(
			(vtkObject *) a0->native.GetPointer()
		);
		v8::Local<v8::Value> argv[1] =
			{ Nan::New(vtkNodeJsNoWrap) };
		v8::Local<v8::Function> cons =
			Nan::New<v8::Function>(VtkClientSocketWrap::constructor);
		v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
		VtkClientSocketWrap *w = new VtkClientSocketWrap();
		w->native.TakeReference(r);
		w->Wrap(wo);
		info.GetReturnValue().Set(wo);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

