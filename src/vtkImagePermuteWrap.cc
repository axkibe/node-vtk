/* this file has been autogenerated by vtkNodeJsWrap */
/* editing this might proof futile */

#define VTK_WRAPPING_CXX
#define VTK_STREAMS_FWD_ONLY
#include <nan.h>


#include "vtkImageResliceWrap.h"
#include "vtkImagePermuteWrap.h"
#include "vtkObjectWrap.h"

using namespace v8;

extern Nan::Persistent<v8::Object> vtkNodeJsNoWrap;
Nan::Persistent<v8::Function> VtkImagePermuteWrap::constructor;
Nan::Persistent<v8::FunctionTemplate> VtkImagePermuteWrap::ptpl;

VtkImagePermuteWrap::VtkImagePermuteWrap()
{ }

VtkImagePermuteWrap::VtkImagePermuteWrap(vtkSmartPointer<vtkImagePermute> _native)
{ native = _native; }

VtkImagePermuteWrap::~VtkImagePermuteWrap()
{ }

void VtkImagePermuteWrap::Init(v8::Local<v8::Object> exports)
{
	if (!constructor.IsEmpty()) return;
	Nan::HandleScope scope;

	v8::Local<v8::FunctionTemplate> tpl = Nan::New<v8::FunctionTemplate>(New);
	VtkImageResliceWrap::Init( exports );
	tpl->Inherit(Nan::New<FunctionTemplate>(VtkImageResliceWrap::ptpl));

	tpl->SetClassName(Nan::New("VtkImagePermuteWrap").ToLocalChecked());
	tpl->InstanceTemplate()->SetInternalFieldCount(1);
	InitTpl(tpl);

	constructor.Reset( tpl->GetFunction() );
	ptpl.Reset( tpl );

	exports->Set(Nan::New("vtkImagePermute").ToLocalChecked(),tpl->GetFunction());
	exports->Set(Nan::New("ImagePermute").ToLocalChecked(),tpl->GetFunction());
}

void VtkImagePermuteWrap::InitTpl(v8::Local<v8::FunctionTemplate> tpl)
{
	Nan::SetPrototypeMethod(tpl, "GetClassName", GetClassName);
	Nan::SetPrototypeMethod(tpl, "getClassName", GetClassName);

	Nan::SetPrototypeMethod(tpl, "IsA", IsA);
	Nan::SetPrototypeMethod(tpl, "isA", IsA);

	Nan::SetPrototypeMethod(tpl, "NewInstance", NewInstance);
	Nan::SetPrototypeMethod(tpl, "newInstance", NewInstance);

	Nan::SetPrototypeMethod(tpl, "SafeDownCast", SafeDownCast);
	Nan::SetPrototypeMethod(tpl, "safeDownCast", SafeDownCast);

	Nan::SetPrototypeMethod(tpl, "SetFilteredAxes", SetFilteredAxes);
	Nan::SetPrototypeMethod(tpl, "setFilteredAxes", SetFilteredAxes);

}

void VtkImagePermuteWrap::New(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	if(!info.IsConstructCall())
	{
		Nan::ThrowError("Constructor not called in a construct call.");
		return;
	}

	if(info.Length() == 0)
	{
		vtkSmartPointer<vtkImagePermute> native = vtkSmartPointer<vtkImagePermute>::New();
		VtkImagePermuteWrap* obj = new VtkImagePermuteWrap(native);		obj->Wrap(info.This());
	}
	else
	{
		Nan::Utf8String s(info[0]);
		if(info[0]->ToObject() != vtkNodeJsNoWrap )
			Nan::ThrowError("Parameter Error");
	}

	info.GetReturnValue().Set(info.This());
}

void VtkImagePermuteWrap::GetClassName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImagePermuteWrap *wrapper = ObjectWrap::Unwrap<VtkImagePermuteWrap>(info.Holder());
	vtkImagePermute *native = (vtkImagePermute *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetClassName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkImagePermuteWrap::IsA(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImagePermuteWrap *wrapper = ObjectWrap::Unwrap<VtkImagePermuteWrap>(info.Holder());
	vtkImagePermute *native = (vtkImagePermute *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		int r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->IsA(
			*a0
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImagePermuteWrap::NewInstance(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImagePermuteWrap *wrapper = ObjectWrap::Unwrap<VtkImagePermuteWrap>(info.Holder());
	vtkImagePermute *native = (vtkImagePermute *)wrapper->native.GetPointer();
	vtkImagePermute * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->NewInstance();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkImagePermuteWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkImagePermuteWrap *w = new VtkImagePermuteWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkImagePermuteWrap::SafeDownCast(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImagePermuteWrap *wrapper = ObjectWrap::Unwrap<VtkImagePermuteWrap>(info.Holder());
	vtkImagePermute *native = (vtkImagePermute *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkObjectWrap *a0 = ObjectWrap::Unwrap<VtkObjectWrap>(info[0]->ToObject());
		vtkImagePermute * r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->SafeDownCast(
			(vtkObject *) a0->native.GetPointer()
		);
		v8::Local<v8::Value> argv[1] =
			{ Nan::New(vtkNodeJsNoWrap) };
		v8::Local<v8::Function> cons =
			Nan::New<v8::Function>(VtkImagePermuteWrap::constructor);
		v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
		VtkImagePermuteWrap *w = new VtkImagePermuteWrap();
		w->native.TakeReference(r);
		w->Wrap(wo);
		info.GetReturnValue().Set(wo);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImagePermuteWrap::SetFilteredAxes(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImagePermuteWrap *wrapper = ObjectWrap::Unwrap<VtkImagePermuteWrap>(info.Holder());
	vtkImagePermute *native = (vtkImagePermute *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() > 1 && info[1]->IsInt32())
		{
			if(info.Length() > 2 && info[2]->IsInt32())
			{
				if(info.Length() != 3)
				{
					Nan::ThrowError("Too many parameters.");
					return;
				}
				native->SetFilteredAxes(
					info[0]->Int32Value(),
					info[1]->Int32Value(),
					info[2]->Int32Value()
				);
				return;
			}
		}
	}
	Nan::ThrowError("Parameter mismatch");
}

