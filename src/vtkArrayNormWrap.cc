/* this file has been autogenerated by vtkNodeJsWrap */
/* editing this might proof futile */

#define VTK_WRAPPING_CXX
#define VTK_STREAMS_FWD_ONLY
#include <nan.h>


#include "vtkArrayDataAlgorithmWrap.h"
#include "vtkArrayNormWrap.h"
#include "vtkObjectWrap.h"

using namespace v8;

extern Nan::Persistent<v8::Object> vtkNodeJsNoWrap;
Nan::Persistent<v8::Function> VtkArrayNormWrap::constructor;

VtkArrayNormWrap::VtkArrayNormWrap()
{ }

VtkArrayNormWrap::VtkArrayNormWrap(vtkSmartPointer<vtkArrayNorm> _native)
{ native = _native; }

VtkArrayNormWrap::~VtkArrayNormWrap()
{ }

void VtkArrayNormWrap::Init(v8::Local<v8::Object> exports)
{
	Nan::HandleScope scope;

	v8::Local<v8::FunctionTemplate> tpl = Nan::New<v8::FunctionTemplate>(New);
	tpl->SetClassName(Nan::New("VtkArrayNormWrap").ToLocalChecked());
	tpl->InstanceTemplate()->SetInternalFieldCount(1);

	VtkObjectBaseWrap::InitTpl(tpl);
	VtkObjectWrap::InitTpl(tpl);
	VtkAlgorithmWrap::InitTpl(tpl);
	VtkArrayDataAlgorithmWrap::InitTpl(tpl);
	InitTpl(tpl);

	constructor.Reset( tpl->GetFunction() );

	exports->Set(Nan::New("vtkArrayNorm").ToLocalChecked(),tpl->GetFunction());
	exports->Set(Nan::New("ArrayNorm").ToLocalChecked(),tpl->GetFunction());
}

void VtkArrayNormWrap::InitTpl(v8::Local<v8::FunctionTemplate> tpl)
{
	Nan::SetPrototypeMethod(tpl, "GetClassName", GetClassName);
	Nan::SetPrototypeMethod(tpl, "getClassName", GetClassName);

	Nan::SetPrototypeMethod(tpl, "GetDimension", GetDimension);
	Nan::SetPrototypeMethod(tpl, "getDimension", GetDimension);

	Nan::SetPrototypeMethod(tpl, "GetInvert", GetInvert);
	Nan::SetPrototypeMethod(tpl, "getInvert", GetInvert);

	Nan::SetPrototypeMethod(tpl, "GetL", GetL);
	Nan::SetPrototypeMethod(tpl, "getL", GetL);

	Nan::SetPrototypeMethod(tpl, "IsA", IsA);
	Nan::SetPrototypeMethod(tpl, "isA", IsA);

	Nan::SetPrototypeMethod(tpl, "NewInstance", NewInstance);
	Nan::SetPrototypeMethod(tpl, "newInstance", NewInstance);

	Nan::SetPrototypeMethod(tpl, "SafeDownCast", SafeDownCast);
	Nan::SetPrototypeMethod(tpl, "safeDownCast", SafeDownCast);

	Nan::SetPrototypeMethod(tpl, "SetDimension", SetDimension);
	Nan::SetPrototypeMethod(tpl, "setDimension", SetDimension);

	Nan::SetPrototypeMethod(tpl, "SetInvert", SetInvert);
	Nan::SetPrototypeMethod(tpl, "setInvert", SetInvert);

	Nan::SetPrototypeMethod(tpl, "SetL", SetL);
	Nan::SetPrototypeMethod(tpl, "setL", SetL);

}

void VtkArrayNormWrap::New(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	if(!info.IsConstructCall())
	{
		Nan::ThrowError("Constructor not called in a construct call.");
		return;
	}

	if(info.Length() == 0)
	{
		vtkSmartPointer<vtkArrayNorm> native = vtkSmartPointer<vtkArrayNorm>::New();
		VtkArrayNormWrap* obj = new VtkArrayNormWrap(native);		obj->Wrap(info.This());
	}
	else
	{
		Nan::Utf8String s(info[0]);
		if(info[0]->ToObject() != vtkNodeJsNoWrap )
			Nan::ThrowError("Parameter Error");
	}

	info.GetReturnValue().Set(info.This());
}

void VtkArrayNormWrap::GetClassName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetClassName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkArrayNormWrap::GetDimension(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetDimension();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkArrayNormWrap::GetInvert(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetInvert();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkArrayNormWrap::GetL(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetL();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkArrayNormWrap::IsA(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		int r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->IsA(
			*a0
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkArrayNormWrap::NewInstance(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	vtkArrayNorm * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->NewInstance();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkArrayNormWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkArrayNormWrap *w = new VtkArrayNormWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkArrayNormWrap::SafeDownCast(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkObjectWrap *a0 = ObjectWrap::Unwrap<VtkObjectWrap>(info[0]->ToObject());
		vtkArrayNorm * r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->SafeDownCast(
			(vtkObject *) a0->native.GetPointer()
		);
		v8::Local<v8::Value> argv[1] =
			{ Nan::New(vtkNodeJsNoWrap) };
		v8::Local<v8::Function> cons =
			Nan::New<v8::Function>(VtkArrayNormWrap::constructor);
		v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
		VtkArrayNormWrap *w = new VtkArrayNormWrap();
		w->native.TakeReference(r);
		w->Wrap(wo);
		info.GetReturnValue().Set(wo);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkArrayNormWrap::SetDimension(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetDimension(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkArrayNormWrap::SetInvert(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetInvert(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkArrayNormWrap::SetL(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkArrayNormWrap *wrapper = ObjectWrap::Unwrap<VtkArrayNormWrap>(info.Holder());
	vtkArrayNorm *native = (vtkArrayNorm *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetL(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

