/* this file has been autogenerated by vtkNodeJsWrap */
/* editing this might proof futile */

#define VTK_WRAPPING_CXX
#define VTK_STREAMS_FWD_ONLY
#include <nan.h>


#include "vtkPolyDataAlgorithmWrap.h"
#include "vtkImageMarchingCubesWrap.h"
#include "vtkObjectWrap.h"

using namespace v8;

extern Nan::Persistent<v8::Object> vtkNodeJsNoWrap;
Nan::Persistent<v8::Function> VtkImageMarchingCubesWrap::constructor;
Nan::Persistent<v8::FunctionTemplate> VtkImageMarchingCubesWrap::ptpl;

VtkImageMarchingCubesWrap::VtkImageMarchingCubesWrap()
{ }

VtkImageMarchingCubesWrap::VtkImageMarchingCubesWrap(vtkSmartPointer<vtkImageMarchingCubes> _native)
{ native = _native; }

VtkImageMarchingCubesWrap::~VtkImageMarchingCubesWrap()
{ }

void VtkImageMarchingCubesWrap::Init(v8::Local<v8::Object> exports)
{
	if (!constructor.IsEmpty()) return;
	Nan::HandleScope scope;

	v8::Local<v8::FunctionTemplate> tpl = Nan::New<v8::FunctionTemplate>(New);
	VtkPolyDataAlgorithmWrap::Init( exports );
	tpl->Inherit(Nan::New<FunctionTemplate>(VtkPolyDataAlgorithmWrap::ptpl));

	tpl->SetClassName(Nan::New("VtkImageMarchingCubesWrap").ToLocalChecked());
	tpl->InstanceTemplate()->SetInternalFieldCount(1);
	InitTpl(tpl);

	constructor.Reset( tpl->GetFunction() );
	ptpl.Reset( tpl );

	exports->Set(Nan::New("vtkImageMarchingCubes").ToLocalChecked(),tpl->GetFunction());
	exports->Set(Nan::New("ImageMarchingCubes").ToLocalChecked(),tpl->GetFunction());
}

void VtkImageMarchingCubesWrap::InitTpl(v8::Local<v8::FunctionTemplate> tpl)
{
	Nan::SetPrototypeMethod(tpl, "ComputeGradientsOff", ComputeGradientsOff);
	Nan::SetPrototypeMethod(tpl, "computeGradientsOff", ComputeGradientsOff);

	Nan::SetPrototypeMethod(tpl, "ComputeGradientsOn", ComputeGradientsOn);
	Nan::SetPrototypeMethod(tpl, "computeGradientsOn", ComputeGradientsOn);

	Nan::SetPrototypeMethod(tpl, "ComputeNormalsOff", ComputeNormalsOff);
	Nan::SetPrototypeMethod(tpl, "computeNormalsOff", ComputeNormalsOff);

	Nan::SetPrototypeMethod(tpl, "ComputeNormalsOn", ComputeNormalsOn);
	Nan::SetPrototypeMethod(tpl, "computeNormalsOn", ComputeNormalsOn);

	Nan::SetPrototypeMethod(tpl, "ComputeScalarsOff", ComputeScalarsOff);
	Nan::SetPrototypeMethod(tpl, "computeScalarsOff", ComputeScalarsOff);

	Nan::SetPrototypeMethod(tpl, "ComputeScalarsOn", ComputeScalarsOn);
	Nan::SetPrototypeMethod(tpl, "computeScalarsOn", ComputeScalarsOn);

	Nan::SetPrototypeMethod(tpl, "GenerateValues", GenerateValues);
	Nan::SetPrototypeMethod(tpl, "generateValues", GenerateValues);

	Nan::SetPrototypeMethod(tpl, "GetClassName", GetClassName);
	Nan::SetPrototypeMethod(tpl, "getClassName", GetClassName);

	Nan::SetPrototypeMethod(tpl, "GetComputeGradients", GetComputeGradients);
	Nan::SetPrototypeMethod(tpl, "getComputeGradients", GetComputeGradients);

	Nan::SetPrototypeMethod(tpl, "GetComputeNormals", GetComputeNormals);
	Nan::SetPrototypeMethod(tpl, "getComputeNormals", GetComputeNormals);

	Nan::SetPrototypeMethod(tpl, "GetComputeScalars", GetComputeScalars);
	Nan::SetPrototypeMethod(tpl, "getComputeScalars", GetComputeScalars);

	Nan::SetPrototypeMethod(tpl, "GetNumberOfContours", GetNumberOfContours);
	Nan::SetPrototypeMethod(tpl, "getNumberOfContours", GetNumberOfContours);

	Nan::SetPrototypeMethod(tpl, "GetValue", GetValue);
	Nan::SetPrototypeMethod(tpl, "getValue", GetValue);

	Nan::SetPrototypeMethod(tpl, "IncrementLocatorZ", IncrementLocatorZ);
	Nan::SetPrototypeMethod(tpl, "incrementLocatorZ", IncrementLocatorZ);

	Nan::SetPrototypeMethod(tpl, "IsA", IsA);
	Nan::SetPrototypeMethod(tpl, "isA", IsA);

	Nan::SetPrototypeMethod(tpl, "NewInstance", NewInstance);
	Nan::SetPrototypeMethod(tpl, "newInstance", NewInstance);

	Nan::SetPrototypeMethod(tpl, "SafeDownCast", SafeDownCast);
	Nan::SetPrototypeMethod(tpl, "safeDownCast", SafeDownCast);

	Nan::SetPrototypeMethod(tpl, "SetComputeGradients", SetComputeGradients);
	Nan::SetPrototypeMethod(tpl, "setComputeGradients", SetComputeGradients);

	Nan::SetPrototypeMethod(tpl, "SetComputeNormals", SetComputeNormals);
	Nan::SetPrototypeMethod(tpl, "setComputeNormals", SetComputeNormals);

	Nan::SetPrototypeMethod(tpl, "SetComputeScalars", SetComputeScalars);
	Nan::SetPrototypeMethod(tpl, "setComputeScalars", SetComputeScalars);

	Nan::SetPrototypeMethod(tpl, "SetNumberOfContours", SetNumberOfContours);
	Nan::SetPrototypeMethod(tpl, "setNumberOfContours", SetNumberOfContours);

	Nan::SetPrototypeMethod(tpl, "SetValue", SetValue);
	Nan::SetPrototypeMethod(tpl, "setValue", SetValue);

}

void VtkImageMarchingCubesWrap::New(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	if(!info.IsConstructCall())
	{
		Nan::ThrowError("Constructor not called in a construct call.");
		return;
	}

	if(info.Length() == 0)
	{
		vtkSmartPointer<vtkImageMarchingCubes> native = vtkSmartPointer<vtkImageMarchingCubes>::New();
		VtkImageMarchingCubesWrap* obj = new VtkImageMarchingCubesWrap(native);		obj->Wrap(info.This());
	}
	else
	{
		Nan::Utf8String s(info[0]);
		if(info[0]->ToObject() != vtkNodeJsNoWrap )
			Nan::ThrowError("Parameter Error");
	}

	info.GetReturnValue().Set(info.This());
}

void VtkImageMarchingCubesWrap::ComputeGradientsOff(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->ComputeGradientsOff();
}

void VtkImageMarchingCubesWrap::ComputeGradientsOn(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->ComputeGradientsOn();
}

void VtkImageMarchingCubesWrap::ComputeNormalsOff(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->ComputeNormalsOff();
}

void VtkImageMarchingCubesWrap::ComputeNormalsOn(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->ComputeNormalsOn();
}

void VtkImageMarchingCubesWrap::ComputeScalarsOff(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->ComputeScalarsOff();
}

void VtkImageMarchingCubesWrap::ComputeScalarsOn(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->ComputeScalarsOn();
}

void VtkImageMarchingCubesWrap::GenerateValues(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() > 1 && info[1]->IsNumber())
		{
			if(info.Length() > 2 && info[2]->IsNumber())
			{
				if(info.Length() != 3)
				{
					Nan::ThrowError("Too many parameters.");
					return;
				}
				native->GenerateValues(
					info[0]->Int32Value(),
					info[1]->NumberValue(),
					info[2]->NumberValue()
				);
				return;
			}
		}
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::GetClassName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetClassName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkImageMarchingCubesWrap::GetComputeGradients(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetComputeGradients();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkImageMarchingCubesWrap::GetComputeNormals(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetComputeNormals();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkImageMarchingCubesWrap::GetComputeScalars(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetComputeScalars();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkImageMarchingCubesWrap::GetNumberOfContours(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	int r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetNumberOfContours();
	info.GetReturnValue().Set(Nan::New(r));
}

void VtkImageMarchingCubesWrap::GetValue(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		double r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->GetValue(
			info[0]->Int32Value()
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::IncrementLocatorZ(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	native->IncrementLocatorZ();
}

void VtkImageMarchingCubesWrap::IsA(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		int r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->IsA(
			*a0
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::NewInstance(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	vtkImageMarchingCubes * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->NewInstance();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkImageMarchingCubesWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkImageMarchingCubesWrap *w = new VtkImageMarchingCubesWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkImageMarchingCubesWrap::SafeDownCast(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkObjectWrap *a0 = ObjectWrap::Unwrap<VtkObjectWrap>(info[0]->ToObject());
		vtkImageMarchingCubes * r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->SafeDownCast(
			(vtkObject *) a0->native.GetPointer()
		);
		v8::Local<v8::Value> argv[1] =
			{ Nan::New(vtkNodeJsNoWrap) };
		v8::Local<v8::Function> cons =
			Nan::New<v8::Function>(VtkImageMarchingCubesWrap::constructor);
		v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
		VtkImageMarchingCubesWrap *w = new VtkImageMarchingCubesWrap();
		w->native.TakeReference(r);
		w->Wrap(wo);
		info.GetReturnValue().Set(wo);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::SetComputeGradients(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetComputeGradients(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::SetComputeNormals(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetComputeNormals(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::SetComputeScalars(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetComputeScalars(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::SetNumberOfContours(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetNumberOfContours(
			info[0]->Int32Value()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkImageMarchingCubesWrap::SetValue(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkImageMarchingCubesWrap *wrapper = ObjectWrap::Unwrap<VtkImageMarchingCubesWrap>(info.Holder());
	vtkImageMarchingCubes *native = (vtkImageMarchingCubes *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		if(info.Length() > 1 && info[1]->IsNumber())
		{
			if(info.Length() != 2)
			{
				Nan::ThrowError("Too many parameters.");
				return;
			}
			native->SetValue(
				info[0]->Int32Value(),
				info[1]->NumberValue()
			);
			return;
		}
	}
	Nan::ThrowError("Parameter mismatch");
}

