/* this file has been autogenerated by vtkNodeJsWrap */
/* editing this might proof futile */

#define VTK_WRAPPING_CXX
#define VTK_STREAMS_FWD_ONLY
#include <nan.h>


#include "vtkStructuredGridAlgorithmWrap.h"
#include "vtkBlankStructuredGridWithImageWrap.h"
#include "vtkObjectWrap.h"
#include "vtkImageDataWrap.h"

using namespace v8;

extern Nan::Persistent<v8::Object> vtkNodeJsNoWrap;
Nan::Persistent<v8::Function> VtkBlankStructuredGridWithImageWrap::constructor;
Nan::Persistent<v8::FunctionTemplate> VtkBlankStructuredGridWithImageWrap::ptpl;

VtkBlankStructuredGridWithImageWrap::VtkBlankStructuredGridWithImageWrap()
{ }

VtkBlankStructuredGridWithImageWrap::VtkBlankStructuredGridWithImageWrap(vtkSmartPointer<vtkBlankStructuredGridWithImage> _native)
{ native = _native; }

VtkBlankStructuredGridWithImageWrap::~VtkBlankStructuredGridWithImageWrap()
{ }

void VtkBlankStructuredGridWithImageWrap::Init(v8::Local<v8::Object> exports)
{
	if (!constructor.IsEmpty()) return;
	Nan::HandleScope scope;

	v8::Local<v8::FunctionTemplate> tpl = Nan::New<v8::FunctionTemplate>(New);
	VtkStructuredGridAlgorithmWrap::Init( exports );
	tpl->Inherit(Nan::New<FunctionTemplate>(VtkStructuredGridAlgorithmWrap::ptpl));

	tpl->SetClassName(Nan::New("VtkBlankStructuredGridWithImageWrap").ToLocalChecked());
	tpl->InstanceTemplate()->SetInternalFieldCount(1);
	InitTpl(tpl);

	constructor.Reset( tpl->GetFunction() );
	ptpl.Reset( tpl );

	exports->Set(Nan::New("vtkBlankStructuredGridWithImage").ToLocalChecked(),tpl->GetFunction());
	exports->Set(Nan::New("BlankStructuredGridWithImage").ToLocalChecked(),tpl->GetFunction());
}

void VtkBlankStructuredGridWithImageWrap::InitTpl(v8::Local<v8::FunctionTemplate> tpl)
{
	Nan::SetPrototypeMethod(tpl, "GetBlankingInput", GetBlankingInput);
	Nan::SetPrototypeMethod(tpl, "getBlankingInput", GetBlankingInput);

	Nan::SetPrototypeMethod(tpl, "GetClassName", GetClassName);
	Nan::SetPrototypeMethod(tpl, "getClassName", GetClassName);

	Nan::SetPrototypeMethod(tpl, "IsA", IsA);
	Nan::SetPrototypeMethod(tpl, "isA", IsA);

	Nan::SetPrototypeMethod(tpl, "NewInstance", NewInstance);
	Nan::SetPrototypeMethod(tpl, "newInstance", NewInstance);

	Nan::SetPrototypeMethod(tpl, "SafeDownCast", SafeDownCast);
	Nan::SetPrototypeMethod(tpl, "safeDownCast", SafeDownCast);

	Nan::SetPrototypeMethod(tpl, "SetBlankingInputData", SetBlankingInputData);
	Nan::SetPrototypeMethod(tpl, "setBlankingInputData", SetBlankingInputData);

}

void VtkBlankStructuredGridWithImageWrap::New(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	if(!info.IsConstructCall())
	{
		Nan::ThrowError("Constructor not called in a construct call.");
		return;
	}

	if(info.Length() == 0)
	{
		vtkSmartPointer<vtkBlankStructuredGridWithImage> native = vtkSmartPointer<vtkBlankStructuredGridWithImage>::New();
		VtkBlankStructuredGridWithImageWrap* obj = new VtkBlankStructuredGridWithImageWrap(native);		obj->Wrap(info.This());
	}
	else
	{
		Nan::Utf8String s(info[0]);
		if(info[0]->ToObject() != vtkNodeJsNoWrap )
			Nan::ThrowError("Parameter Error");
	}

	info.GetReturnValue().Set(info.This());
}

void VtkBlankStructuredGridWithImageWrap::GetBlankingInput(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkBlankStructuredGridWithImageWrap *wrapper = ObjectWrap::Unwrap<VtkBlankStructuredGridWithImageWrap>(info.Holder());
	vtkBlankStructuredGridWithImage *native = (vtkBlankStructuredGridWithImage *)wrapper->native.GetPointer();
	vtkImageData * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetBlankingInput();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkImageDataWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkImageDataWrap *w = new VtkImageDataWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkBlankStructuredGridWithImageWrap::GetClassName(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkBlankStructuredGridWithImageWrap *wrapper = ObjectWrap::Unwrap<VtkBlankStructuredGridWithImageWrap>(info.Holder());
	vtkBlankStructuredGridWithImage *native = (vtkBlankStructuredGridWithImage *)wrapper->native.GetPointer();
	char const * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->GetClassName();
	info.GetReturnValue().Set(Nan::New(r).ToLocalChecked());
}

void VtkBlankStructuredGridWithImageWrap::IsA(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkBlankStructuredGridWithImageWrap *wrapper = ObjectWrap::Unwrap<VtkBlankStructuredGridWithImageWrap>(info.Holder());
	vtkBlankStructuredGridWithImage *native = (vtkBlankStructuredGridWithImage *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsInt32())
	{
		Nan::Utf8String a0(info[0]);
		int r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->IsA(
			*a0
		);
		info.GetReturnValue().Set(Nan::New(r));
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkBlankStructuredGridWithImageWrap::NewInstance(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkBlankStructuredGridWithImageWrap *wrapper = ObjectWrap::Unwrap<VtkBlankStructuredGridWithImageWrap>(info.Holder());
	vtkBlankStructuredGridWithImage *native = (vtkBlankStructuredGridWithImage *)wrapper->native.GetPointer();
	vtkBlankStructuredGridWithImage * r;
	if(info.Length() != 0)
	{
		Nan::ThrowError("Too many parameters.");
		return;
	}
	r = native->NewInstance();
	v8::Local<v8::Value> argv[1] =
		{ Nan::New(vtkNodeJsNoWrap) };
	v8::Local<v8::Function> cons =
		Nan::New<v8::Function>(VtkBlankStructuredGridWithImageWrap::constructor);
	v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
	VtkBlankStructuredGridWithImageWrap *w = new VtkBlankStructuredGridWithImageWrap();
	w->native.TakeReference(r);
	w->Wrap(wo);
	info.GetReturnValue().Set(wo);
}

void VtkBlankStructuredGridWithImageWrap::SafeDownCast(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkBlankStructuredGridWithImageWrap *wrapper = ObjectWrap::Unwrap<VtkBlankStructuredGridWithImageWrap>(info.Holder());
	vtkBlankStructuredGridWithImage *native = (vtkBlankStructuredGridWithImage *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkObjectWrap *a0 = ObjectWrap::Unwrap<VtkObjectWrap>(info[0]->ToObject());
		vtkBlankStructuredGridWithImage * r;
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		r = native->SafeDownCast(
			(vtkObject *) a0->native.GetPointer()
		);
		v8::Local<v8::Value> argv[1] =
			{ Nan::New(vtkNodeJsNoWrap) };
		v8::Local<v8::Function> cons =
			Nan::New<v8::Function>(VtkBlankStructuredGridWithImageWrap::constructor);
		v8::Local<v8::Object> wo = cons->NewInstance(1, argv);
		VtkBlankStructuredGridWithImageWrap *w = new VtkBlankStructuredGridWithImageWrap();
		w->native.TakeReference(r);
		w->Wrap(wo);
		info.GetReturnValue().Set(wo);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

void VtkBlankStructuredGridWithImageWrap::SetBlankingInputData(const Nan::FunctionCallbackInfo<v8::Value>& info)
{
	VtkBlankStructuredGridWithImageWrap *wrapper = ObjectWrap::Unwrap<VtkBlankStructuredGridWithImageWrap>(info.Holder());
	vtkBlankStructuredGridWithImage *native = (vtkBlankStructuredGridWithImage *)wrapper->native.GetPointer();
	if(info.Length() > 0 && info[0]->IsObject())
	{
		VtkImageDataWrap *a0 = ObjectWrap::Unwrap<VtkImageDataWrap>(info[0]->ToObject());
		if(info.Length() != 1)
		{
			Nan::ThrowError("Too many parameters.");
			return;
		}
		native->SetBlankingInputData(
			(vtkImageData *) a0->native.GetPointer()
		);
		return;
	}
	Nan::ThrowError("Parameter mismatch");
}

